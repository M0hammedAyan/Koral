apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: koral-system

resources:
  - pgbackrest-config.yaml
  - pgbackrest-backup-cronjobs.yaml
  - pgbackrest-restore-verify.yaml
  - pgbouncer.yaml

commonLabels:
  app.kubernetes.io/part-of: koral
  app.kubernetes.io/component: database-resilience
  app.kubernetes.io/version: "1.0.0"
