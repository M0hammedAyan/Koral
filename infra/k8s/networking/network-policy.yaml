apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: koral-allow-internal
  namespace: koral-system
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: koral-system
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: koral-system
    # Allow reaching Prometheus + Fluentd in ai-observability
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ai-observability
    # Allow DNS
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
    # Allow Kubernetes API server
    - ports:
        - port: 443
          protocol: TCP
        - port: 6443
          protocol: TCP
